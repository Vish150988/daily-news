# CLAUDE.md

> Auto-generated by CrossAgentMemory. Do not edit manually -- run `crossagentmemory sync` instead.

## Project

Project: daily-news

Working directory: C:\Users\vishn\daily-news

## Conventions & Preferences

_None recorded yet._

## Recent Decisions

- HomeView refresh throttle: _last_refresh timestamp gate — skips background fetch if last refresh was within 300 seconds (5 min). Prevents unbounded thread accumulation when HomeView is re-created on every route change.
- Flet APK build: remove flet and pytest from requirements.txt. Flet is provided by the build environment automatically. pytest is dev-only. Only include runtime deps that have Android ARM64 wheels.
- Flet APK build: do NOT pre-install Flutter via subosito/flutter-action — Flet 0.84.0 manages its own Flutter version internally. Pre-installing Flutter 3.41.7 causes version incompatibility. Let flet build apk download its own Flutter to ~/.flet.
- Flet APK build: trafilatura cannot be included in requirements.txt for Android APK — its lxml dependency has no pre-built wheels for ARM64. Solution: make trafilatura optional (ImportError fallback) and use pure-Python stdlib extractor on mobile.

## Known Issues

- Flet 0.84.0: use ft.run(main) not ft.app(target=main) — ft.app() is deprecated since 0.80.0.
- Flet 0.84.0 critical: ft.alignment.center, ft.alignment.top_left, ft.alignment.bottom_right do NOT exist. Use ft.Alignment(0,0), ft.Alignment(-1,-1), ft.Alignment(1,1) instead.
- Flet 0.84.0 critical: page.go() is deprecated and does NOT trigger on_route_change. Fix: bypass routing entirely, directly manage page.views and call view.did_mount() explicitly after page.update().

## Architecture

- Visual design: Bold & Vibrant theme. Dark base #18181b, card bg #27272a. Category accent colors: World=#e63946, Tech=#f4a261, Data-AI=#a78bfa, Business=#4ade80, Florida=#38bdf8, US-Politics=#fb923c, Long-Form=#e879f9. Hero card gradient red to orange.
- RSS feeds: 5 World (Reuters/BBC/NYT/Guardian/AlJazeera), 4 Tech (TechCrunch/Wired/Verge/MIT), 5 Data-AI (HackerNews/ArsTechnica/KDnuggets/VentureBeat/NewStack), 3 Business (WSJ/CNBC/Fortune), 3 Florida (PalmBeach/SunSentinel/CBSMiami), 4 US-Politics (NPR/Politico/ProPublica/AP), 3 LongForm (Atlantic/Longreads/Smithsonian). Fetched in parallel via ThreadPoolExecutor(max_workers=10).
- Storage layer: DB_PATH = Path.home() / .daily-news / news.db. Tables: articles (id=MD5(url), title, url, source, category, excerpt, published_at, fetched_at) and bookmarks (article_id PK, saved_at). Uses contextlib.closing to close connections explicitly.
- GitHub repo: https://github.com/Vish150988/daily-news — master branch. GitHub Actions workflow at .github/workflows/build-apk.yml builds Android APK on every push using ubuntu-latest + Java 17 + Flet-managed Flutter.
- Daily News app: cross-platform Python/Flet news reader. 27 RSS feeds across 7 categories. Features: hero card, category chips, in-app reader, SQLite bookmarks. Repo: https://github.com/Vish150988/daily-news

---

*Last updated: 2026-04-23T02:53:14.655211+00:00*
